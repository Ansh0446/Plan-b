/**
 * Placeholder root route so the app builds and runs. Real landing-page
 * composition (Patterns/Composites) is out of scope for repository setup
 * and lands in a future milestone per Phase 1 §5 (Template Architecture).
 */
export default function RootPage() {
  return null;
}
