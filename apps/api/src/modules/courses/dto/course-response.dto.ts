/**
 * Re-exported from `@plan-b/shared-types` — the canonical definition
 * (Phase 1 §1). Kept as a local re-export so every other file in this
 * module can `import type { CourseResponseDto } from './dto/course-response.dto'`
 * without needing to know the contract actually lives in the shared package.
 */
export type { CourseResponseDto } from '@plan-b/shared-types';
