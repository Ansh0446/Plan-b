{
  "name": "@plan-b/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "The single legal definition of every cross-boundary data shape shared between apps/web and apps/api.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@plan-b/config": "workspace:*",
    "eslint": "^9.15.0",
    "typescript": "^5.6.3"
  }
}
